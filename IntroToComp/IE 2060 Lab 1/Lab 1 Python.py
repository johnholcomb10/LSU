import math

# Assume x is defined as double with example faule of 2.5
x = 2.5

# Compute expression and assign to Z
z = (x ** 2 + math.sqrt(x) + math.log(12)) / (math.sin(x) + 6)

# Display the result
print("Z: ", z)